apiVersion: v1
kind: Service
metadata:
  name: legal-qa-platform-api
  labels:
    app.kubernetes.io/name: legal-qa-platform
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: legal-qa-platform
    app.kubernetes.io/component: api
  ports:
    - name: http
      port: 80
      targetPort: http
